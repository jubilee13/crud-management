name: First Workflow
on:
  push:
    branches: ["staging" ]
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print welcome greeting
        run: echo "Hello world!"
      - name: Print goodbye
        run: echo "Goodbye guys!"
